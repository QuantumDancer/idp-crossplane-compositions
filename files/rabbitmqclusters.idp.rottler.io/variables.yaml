{{- $xrName := .observed.composite.resource.metadata.name -}}
{{- $namespace := .observed.composite.resource.metadata.namespace -}}
{{- $ha := .observed.composite.resource.spec.ha | default false -}}
{{- $version := .observed.composite.resource.spec.version | default "3.13" -}}
{{- $replicas := 1 -}}
{{- if $ha -}}
  {{- $replicas = 3 -}}
{{- end -}}
{{- $storageSizeMap := dict "small" "1Gi" "medium" "10Gi" "large" "20Gi" "x-large" "50Gi" -}}
{{- $storageSize := index $storageSizeMap (.observed.composite.resource.spec.storageSize | default "small") -}}

{{- $labels := dict
  "app.kubernetes.io/name" "rabbitmq-cluster"
  "app.kubernetes.io/instance" $xrName
  "app.kubernetes.io/component" "messaging"
  "app.kubernetes.io/managed-by" "crossplane"
  "idp.rottler.io/team" (index .observed.composite.resource.metadata.labels "idp.rottler.io/team")
-}}
