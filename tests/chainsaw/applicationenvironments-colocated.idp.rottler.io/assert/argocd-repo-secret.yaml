apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: repo-team-a-order-management-order-worker-deployment
  namespace: argocd
spec:
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
