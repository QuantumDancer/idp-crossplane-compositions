apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: watermarkflow-edge
spec:
  parentRefs:
    - name: external
      namespace: gateway
  # expose.host is used verbatim — the base domain is NOT appended.
  hostnames:
    - edge.custom.example.com
